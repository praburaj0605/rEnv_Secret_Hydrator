# esh-providers-docker

Docker `/run/secrets` provider for **Env-Secret-Hydrator**.

Usually consumed via the [`env-secret-hydrator`](https://crates.io/crates/env-secret-hydrator) façade (`docker` feature).

## License

MIT OR Apache-2.0
